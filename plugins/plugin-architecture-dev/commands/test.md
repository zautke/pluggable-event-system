---
description: Run the Testing phase - author and run an automated suite mapping each expected behavior to a test, and audit for untested behaviors. Writes .sdlc/testing/.
argument-hint: <what to test (defaults to the current run)>
---

Use the `plugin-architecture-dev:phase-testing` skill to test:

$ARGUMENTS
