---
description: Create an AI engineering project snapshot using the shared plugin contracts. Writes or describes ProjectSnapshot artifacts.
argument-hint: <project path or ingestion goal>
---

Use the `plugin-architecture-dev:ai-system-decomposition` skill in ingestion mode:

$ARGUMENTS
